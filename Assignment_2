 Ques.1. What do you mean by structures?
Sol.  In  C, a structure is a user-defined data type which is collection of variables of different data types under a single name .

#include <stdio.h>

struct Student {// declaration of structure
    int id;
    char name[20];
    float marks;
};

int main() {
    struct Student s1 = {1, "John", 85.5};
    printf("ID: %d\nName: %s\nMarks: %.2f\n", s1.id, s1.name, s1.marks);
    return 0;
}


-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Ques2. What do you mean by Array & Pointers Array?
Sol. Array:

An array is a collection of elements of the same data type, stored in contiguous memory locations.

Pointer Array (Array of Pointers):

An array of pointers is a collection of memory addresses, where each element points to some data (like integers or strings).

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Ques3. Compare two structures variables by using the comparison operator ( Not equal & equal).
Sol.
#include <stdio.h>
#include <string.h>

struct Student {
    int id;
    char name[20];
};

int main() {
    struct Student s1 = {1, "John"};
    struct Student s2 = {1, "John"};

    if (s1.id == s2.id && strcmp(s1.name, s2.name) == 0) // comparing values
        printf("Structures are equal.\n");
    else
        printf("Structures are not equal.\n");

    return 0;
}

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Ques4. Perform an arithmetic operations on structures.
Sol.
#include <stdio.h>

struct Point {
    int x;
    int y;
};

int main() {
    struct Point p1 = {10, 20};
    struct Point p2 = {5, 15};
    struct Point sum, diff;

    // Arithmetic operations on structure members
    sum.x = p1.x + p2.x;
    sum.y = p1.y + p2.y;

    diff.x = p1.x - p2.x;
    diff.y = p1.y - p2.y;

    printf("Sum: x = %d, y = %d\n", sum.x, sum.y);
    printf("Difference: x = %d, y = %d\n", diff.x, diff.y);

    return 0;
}
